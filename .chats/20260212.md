# new feature

1. add command list, List all features that have been planned.

2. add command status, Show detailed information about a specific feature.

# issue

仔细阅读代码， 查看问题的 root cause， 解释 root cause 和 list fix plan。不要做任何事情。


for each crate, it should have an API.md doc in their folder to illustrate the public interface of the crate， so its easy to understand and use.

input should use rustyline to support inline editing and basic hotkeys to move begin/end,delete words,etc

在 plan 阶段，理解用户的意图，根据这个意图构建 design.md spec， 而非完成用户期待的工作；现在的这个对话里，用户希望生成API.md，结果 assistant返回了哪些crate缺API，并提示可以进一步帮助创建，这里应该先有一个设计跟用户讨论，一旦用户确认，应该把这个设计写入到合适的地方，并创建一个 initial state.yml


如何 plan 阶段输入框，用户输入内容换行后，显示不对
